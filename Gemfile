source "https://rubygems.org"

# Pin fastlane for reproducible Play Store uploads / screenshot tooling.
# Usage:  bundle install  &&  cd android && bundle exec fastlane <lane>
# (fastlane also works without bundler if it is installed globally.)
gem "fastlane"
